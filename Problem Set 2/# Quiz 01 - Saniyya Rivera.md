# Quiz 01 - Saniyya Rivera

## 1. What is a repository when using Git and GitHub?

- A project containing files and their version history. **<-- This**
- A computer connected to your laptop.
- A programming language.
- A GitHub account.

## 2. What does `commit` mean when using Git?

- Capture a snapshot of the project's staged changes. **<-- This**
- Download a repository from GitHub.
- Upload a repository to GitHub.
- Delete changes from a repository.

## 3. What does `pull` do when using Git?

- Downloads changes from a remote repository and updates the local repository. **<-- This**
- Saves a snapshot of staged changes.
- Uploads local changes to a remote repository.
- Creates a new repository.

## 4. Which filename does GitHub automatically display as the main Markdown document for a repository?

- `README.md` **<-- This**
- `DOC.md`
- `README.txt`
- `main.md`

## 5. What is the binary number `10000011` in decimal?
*Please explain how you got the answer.*

## 6. What does the ASCII sequence `76 77 83 67 45 50 54 49` represent? 
*Hint: Look up ASCII table*

**LMSC-261**

## 7. What does it mean that Python is an interpreted programming language?

- Python code can be run without manually compiling it first. **<-- This**
- Python code must be converted to machine code by the programmer before it can run.
- Python code can only run inside a web browser.
- Python code does not need an interpreter to run.

## 8. What is sequential execution?

- Instructions execute in the order they appear in the program. **<-- This**
- Instructions repeat forever.
- Instructions execute only when a condition is true.
- Instructions execute in random order.

## 9. What is a condition in programming?

- An expression that can determine whether an action should occur. **<-- This**
- A command that repeats code forever.
- A collection of files.
- A command that prints text.

## 10. Which statement correctly assigns the value `26` to a variable named `abc` in Python?

- `abc = 26` **<-- This**
- `abc == 26`
- `26 = abc`
- `abc : 26`

## 11. What does the following Python statement do?

`counter += 1`

- Increases `counter` by 1. **<-- This**
- Sets `counter` equal to 1.
- Compares `counter` to 1.
- Prints `counter`.

## 12.What does each operator do in Python?

- `==` **<-- Compare**
- `=` **<-- Assign**
- `-=` **<-- Subtract and assign**

## 13. What is important about the indentation in the following code?

```python
if x < y:
    print("x is less than y")
```

- It indicates that the `print()` statement belongs to the `if` block. **<-- This**
- It is only used to make the code look nicer.
- It causes the condition to repeat.
- It turns the statement into a comment.

## 14. What is a `while` loop?
**Repeat until something changes**
## 15. What is a `for` loop?
**Repeat a certain amount of times**
## 16. What values are produced by a) `range(5)` and b) `range(5,10)`?
**a) 5** 

**b) 5, 6, 7, 8, 9, 10**
## 17. What does the `%` operator do in Python?
**Calculate the remainder**
## 18. Which statement correctly creates a single line comment in Python?

- `# This is a comment` **<-- This**
- `// This is a comment`
- `/* This is a comment */`
- `<!-- This is a comment -->`

## 19. What is Conda used for in this class?

- Writing Python code instead of VS Code.
- Storing GitHub repositories.
- Managing Python environments and packages. **<-- This**
- Converting Python into JavaScript.

## 20. What does `if __name__ == "__main__":` check in Python?

- Whether the program contains a function named `main`.
- Whether Python is installed on the computer.
- Whether the program has any errors.
- Whether the current Python file is being run directly as the main program. **<-- This**